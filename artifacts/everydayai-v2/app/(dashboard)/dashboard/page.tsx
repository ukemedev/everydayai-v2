export default function DashboardPage() {
  return (
    <div style={{ padding: "28px" }}>
      <div style={{ fontSize: "9px", color: "var(--text-muted)", letterSpacing: "0.1em", textTransform: "uppercase", marginBottom: "18px" }}>
        // dashboard
      </div>
      <h1 style={{ fontFamily: "var(--font-sans)", fontSize: "24px", fontWeight: 700, color: "var(--white)", marginBottom: "8px" }}>
        Welcome back
      </h1>
      <p style={{ fontSize: "12px", color: "var(--text-muted)" }}>
        Your agents are ready.
      </p>
    </div>
  )
}
